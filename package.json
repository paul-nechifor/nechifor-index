{
  "name": "nechifor-index",
  "version": "0.3.0",
  "author": "Paul Nechifor <paul@nechifor.net> (http://nechifor.net/)",
  "scripts": {
    "build": "intercessor",
    "start": "node build/app.js",
    "intercessor-prepare": "git submodule init && git submodule update && npm install && npm run build"
  },
  "dependencies": {
    "coffee-script": "^1.7.1",
    "cv-info": ">=0.3.0",
    "gulp": "^3.8.0",
    "gulp-jade": "^0.5.0",
    "intercessor": ">=0.3.0"
  },
  "license": "MIT"
}
